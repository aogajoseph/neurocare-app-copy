import { View, Text, StyleSheet, ScrollView, TouchableOpacity } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { router } from 'expo-router';

export default function EmergencyScreen() {
  const insets = useSafeAreaInsets();

  return (
    <View style={[styles.root, { paddingTop: insets.top }]}>
      {/* ───── Header ───── */}
      <View style={styles.header}>
        <TouchableOpacity
          onPress={() => router.back()}
          style={styles.backButton}
          hitSlop={10}
        >
          <Ionicons name="arrow-back" size={22} color="#111827" />
        </TouchableOpacity>

        <Text style={styles.title}>Emergency Resources</Text>
      </View>

      {/* ───── Content ───── */}
      <ScrollView
        contentContainerStyle={[
          styles.content,
          { paddingBottom: insets.bottom + 24 },
        ]}
        showsVerticalScrollIndicator={false}
      >
        <Text style={styles.paragraph}>
          Emergencies can arise suddenly, and knowing who to contact and how to respond is vital for safety and timely care.
        </Text>

        <Text style={styles.subTitle}>Medical Emergencies</Text>
        <Text style={styles.paragraph}>
          Contact local hospitals, clinics, or emergency medical services immediately. Keep important medical information accessible.
        </Text>

        <Text style={styles.subTitle}>Safety Concerns</Text>
        <Text style={styles.paragraph}>
          For threats to personal safety, abuse, or accidents, reach out to authorities, social services, or dedicated helplines.
        </Text>

        <Text style={styles.subTitle}>Hotlines & Helplines</Text>
        <Text style={styles.paragraph}>
          Maintain a list of emergency numbers, including local police, ambulance, fire services, and support hotlines relevant to neurological conditions.
        </Text>

        <Text style={styles.paragraph}>
          Additional emergency resources will be updated dynamically based on backend content, local availability, and user preferences.
        </Text>
      </ScrollView>
    </View>
  );
}

const styles = StyleSheet.create({
  root: {
    flex: 1,
    backgroundColor: '#FFFFFF',
  },

  /* Header */
  header: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingHorizontal: 16,
    paddingVertical: 14,
    borderBottomWidth: 1,
    borderBottomColor: '#E5E7EB',
    backgroundColor: '#FFFFFF',
  },
  backButton: {
    marginRight: 12,
  },
  title: {
    fontSize: 18,
    fontWeight: '600',
    color: '#111827',
  },

  /* Content */
  content: {
    paddingHorizontal: 16,
    paddingTop: 16,
  },
  subTitle: {
    fontSize: 16,
    fontWeight: '600',
    color: '#aa2078',
    marginBottom: 8,
  },
  paragraph: {
    fontSize: 15,
    lineHeight: 22,
    color: '#374151',
    marginBottom: 16,
  },
});